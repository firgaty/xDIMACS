package parselib;

public enum Symbol {
    LCURV, RCURV, LPAR, RPAR, NUM, ZERO, ENDL,
    NEG, PROBLEM, COMMA, SEMICOLON, COLON,
    COMMENT, COMMENTEND, LINE, WORD,
    AND, OR, IF,
    LT, GT, GEQ, LEQ, EQ, LOR, LAND, NOT, 
    VAR, ASSIGN,
    DIV, MUL, SUB, ADD, MOD;
}